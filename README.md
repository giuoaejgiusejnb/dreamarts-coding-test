# 最長片道きっぷの旅 - コーディングテスト提出コード

株式会社ドリーム・アーツ 2027年新卒 エンジニア採用コーディングテストの提出物です。
駅と駅を結ぶ鉄道路線網において、同じ点を2回通らずにもっとも長い距離を移動する経路を探索するプログラムです。

## 目次
- [1. 実行環境](#1-実行環境)
- [2. 実行手順](#2-実行手順)
- [3. 計算量について](#3-計算量について)

## 1. 実行環境

特定のOSやハードウェアへの依存を排除するため、Pythonの標準ライブラリのみを用いて実装しています。

* **OS:** Windows 11 Home 64-bit (クロスプラットフォームを意識し、Linux環境等でも動作するよう標準入出力を実装しています)
* **言語:** Python 3.12
* **外部ライブラリ:** 不要（標準ライブラリ `sys`, `dataclasses` のみ使用）

## 2. 実行手順

お使いの環境（ターミナル・シェル）に合わせて、標準入力にテストデータを流し込んで実行してください。

### Mac / Linux（ターミナル）または Windows（コマンドプロンプト）の場合
```bash
python main.py < input.txt
```

### Windows（PowerShell）の場合
```powershell
Get-Content input.txt | python main.py
```


## 3. 計算量について

本プログラムでは、最長片道きっぷ問題の解法として、深さ優先探索（DFS）による全探索アルゴリズムを採用しました。駅の総数を $N$ とすると、探索の深さは最大 $N$ であり、最悪の場合の計算量は $O(N!)$ となります。各ステップにおける経路の重複確認（訪問済みリストの検索）に $O(N)$ を要するため、全体の計算量は $O(N \cdot N!)$ と評価できます。理論上は階乗時間オーダーの探索コストを要しますが、本課題の「鉄道路線網」というデータ特性に着目し、このアプローチを採用しました。現実の鉄道網は各駅の次数が平均的に小さい「疎グラフ」であるため、探索の早期段階で行き止まりに到達し、探索空間が効率的に枝刈りされます。この性質により、一般的な規模の路線網であれば実用的な時間内で確実に最適解を導き出せると判断しました。
